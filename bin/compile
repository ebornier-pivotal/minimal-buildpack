#!/bin/sh

echo "detected minimal.sh"

echo "tutu" > /root/test.log
sudo cat /root/test.log

# abort if script is empty
if [ ! -s $1/minimal.sh ]; then
  echo "aborting due to empty minimal.sh"
  exit 1
fi

